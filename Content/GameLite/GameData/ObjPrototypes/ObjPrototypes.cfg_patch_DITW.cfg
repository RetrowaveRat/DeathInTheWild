Player : struct.begin {bpatch}

   // CHANGE: Reduce stamina cost of sprinting and jumping
   StaminaPerAction : struct.begin {bpatch}
      Sprint = 3
      Jump = 10
   struct.end

   // CHANGE: Remove survival mechanics (hunger, thirst, sleep)
   VitalParams : struct.begin {bpatch}
      RegenHungerPoints = -20
      RegenThirstPoints = -20
      RegenSleepinessPoints = -20
   struct.end

   // CHANGE: Increase player vaulting ability
   VaultingParams : struct.begin {bpatch}
      MaxAngle = 125
      MaxTestDistance = 80
      StartDistance = 20
      VaultOverMaxDepth = 25
      VaultOverLandOffset = 100
      MaxObstacleHeight = 225
      FrontSearchRadiusModifier = 2
      DepthTraceRadiusModifier = 1
      LandingMinHeight = 10
      MaxWindowDetectionIterations = 20
      MaxLandingDetectionIterations = 10
      MaxLandingOffset = 200
      LandingMaxSlope = 90
   struct.end

struct.end
