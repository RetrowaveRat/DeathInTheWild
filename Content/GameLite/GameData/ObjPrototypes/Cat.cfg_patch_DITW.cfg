Bayun : struct.begin {bpatch}

   // CHANGE: Cat (Bayun) should trigger anomalies
   ShouldTriggerAnomalies = true
   
   VitalParams : struct.begin {bpatch}
      // CHANGE: Reduced Cat (Bayun) Health by 50%
      MaxHP = 200
   struct.end

   Protection : struct.begin {bpatch}
      // CHANGE: Reduce Cat (Bayun) Armor by 50%
      Strike = 1.5
   struct.end

struct.end
